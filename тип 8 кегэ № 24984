from itertools import*
from string import printable

a = printable[:16]

k = 0

for x in product(a,repeat = 5):
    s = ''.join(x)
    if s[0] != '0':
        if all(s.count(c) <= 2 for c in s) and any(c in '149' for c in s):
            k += 1
print(k)
